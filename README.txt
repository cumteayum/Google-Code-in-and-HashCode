the "addtoreg.py" file is a python script that is meanyt for adding the min program to the startup programs and modifying windows registry.

the "google code-in task #@2" is the main file which you can execut directly if you want.

the main file is binded with the "addtoreg" file so that it could run when the system starts.

made with Tkinter,OS,webbrowser module.
